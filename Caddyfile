# DSH HTTPS 网关（Caddy）：解决"局域网 HTTP 访问时 crypto.randomUUID 不可用"问题
#
# 浏览器只在 HTTPS 或 localhost 下才提供 crypto.randomUUID，DSH 前端靠它生成消息/RPC id，
# 所以 http://192.168.x.x:3080 这类明文 HTTP 访问时聊天/选模型/选目录全都会报
# "crypto.randomUUID is not a function"。套一层 HTTPS（自签证书）即可全部恢复。
#
# 使用：浏览器打开 https://<NAS的IP>:3443
# 第一次会提示"证书不受信任"——点「高级 → 继续前往」即可（自签证书，正常现象）。
# 之后所有功能可用。

{
  auto_https off
}

:3443 {
  # 修复 Windows schannel 握手报 SEC_E_INTERNAL_ERROR（"无法连接到本地安全机构"）：
  # 1) key_type rsa2048 —— Caddy 默认的 ECDSA 自签证书与 schannel 兼容性差，改 RSA；
  # 2) protocols tls1.2 —— 只协商 TLS 1.2（局域网服务足够），绕开 schannel 在 TLS 1.3
  #    与自签证书组合下的已知问题。两者叠加后 Windows curl/浏览器握手应恢复正常。
  tls internal {
    key_type rsa2048
    protocols tls1.2
  }
  reverse_proxy dsh:3080
}
